import Alert from 'react-bootstrap/Alert';

function ALertBox() {
  return (
    <Alert variant="danger">
      <p>
        Remove Mobile from Portrait Mode, the game is best in Landscape Mode.
      </p>
    </Alert>
  );
}

export default ALertBox;